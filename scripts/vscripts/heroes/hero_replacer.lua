function nova( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_sniper", 600, 0)
end

function cursed( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_bloodseeker", 600, 0)
end

function dw( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_dark_willow", 600, 0)
end

function jugger( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_juggernaut", 600, 0)
end


function drow( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_drow_ranger", 600, 0)
end

function dk( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_dragon_knight", 600, 0)
end


function ta( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_templar_assassin", 600, 0)
end

function axe( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_axe", 600, 0)
end


function void( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_faceless_void", 600, 0)
end

function meepo( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_meepo", 600, 0)
end

function tb( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_terrorblade", 600, 0)
end

function naga( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_naga_siren", 600, 0)
end

function pa( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_phantom_assassin", 600, 0)
end

function pl( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_phantom_lancer", 600, 0)
end

function raste( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_shadow_shaman", 600, 0)
end

function phoenix( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_phoenix", 600, 0)
end

function brax( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_storm_spirit", 600, 0)
end

function naix( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_life_stealer", 600, 0)
end

function es( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_ember_spirit", 600, 0)
end

function zulf( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_skywrath_mage", 600, 0)
end

function titan( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_elder_titan", 600, 0)
end

function db( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_dawnbreaker", 600, 0)
end

function sk( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_sand_king", 600, 0)
end

function uther( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_omniknight", 600, 0)
end

function rubick( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_rubick", 600, 0)
end

function luna( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_luna", 600, 0)
end

function warlock( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_warlock", 600, 0)
end


function abaddon( keys )
local target = keys.target
local caster = keys.caster
local ability = keys.ability
local ability_level = ability:GetLevel() - 1
local gold = caster:GetGold()
PlayerResource:ReplaceHeroWith(caster:GetPlayerID(), "npc_dota_hero_abaddon", 600, 0)
end

